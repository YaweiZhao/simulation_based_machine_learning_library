clear;
%plot loss
loss = load('loss.mat');
loss_data = loss.loss;
num_iter = 1:10;%number of iterations
plot(num_iter,loss_data);

%plot the change of x
delta_x = load('delta_x');
delta_x_data = delta_x.delta_x